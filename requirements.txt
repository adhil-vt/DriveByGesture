# ── DriveByGesture Runtime Dependencies ──────────────────────────────────────
# Install with: pip install -r requirements.txt

# GUI framework
PySide6>=6.7

# Computer vision / gesture tracking
opencv-python>=4.9
mediapipe>=0.10

# Virtual Xbox controller (requires ViGEmBus driver on Windows)
vgamepad>=0.1.0

# Config hot-reload
watchdog>=4.0

# System diagnostics
psutil>=5.9

# Image utilities (icon fallback)
Pillow>=10.0

# ── Packaging (dev only) ──────────────────────────────────────────────────────
# pip install pyinstaller>=6.22

# ── Dev / test dependencies (install with: pip install -r requirements-dev.txt)
# pytest>=8.0
# pytest-cov>=5.0
# pytest-mock>=3.12
# hypothesis>=6.100
# mypy>=1.9
# ruff>=0.4
